# Generated per-run test/report artifacts — regenerated by every
# `npm test` / `npm run test:regression` run, so tracking them just turns
# every local run into noise in `git status` and a diff nobody reviews.
allure-results/
allure-report/
playwright-report/
test-results/
